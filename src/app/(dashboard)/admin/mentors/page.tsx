import { RoutePlaceholder } from "@/components/layout/route-placeholder";

export default function MentorManagementRoute() {
  return (
    <RoutePlaceholder
      description="Mentor management will support capacity limits, availability toggles, manual reassignment, and admin overrides."
      eyebrow="Mentors"
      title="Mentor management foundation"
    />
  );
}
