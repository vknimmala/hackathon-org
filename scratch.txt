================================================================================
CODE REVIEW & EXECUTION SIMULATION
================================================================================
Date: 2026-05-13
Test Case: Revised Phase 1 participant idea intake and approved team registration
Command: Manual paper-based simulation before implementation and validation

================================================================================
INITIAL STATE
================================================================================
ENVIRONMENT:
- Next.js App Router routes `/`, `/register/idea`, and `/register/team`
- Shared theme tokens loaded from `src/app/globals.css`
- React Hook Form and Zod available for registration forms
- Supabase writes happen through server actions

FILE SYSTEM:
- `src/app/(marketing)/page.tsx` owns the public landing route.
- `src/app/register/idea/page.tsx` will own participant idea intake.
- `src/app/register/team/page.tsx` will own approved-idea team creation.
- `src/validations/registration.ts` will own both idea and team schemas.
- `supabase/migrations` will describe the persisted idea-first flow.

================================================================================
EXECUTION FLOW - STEP BY STEP
================================================================================

STEP 1: Actual hackathon landing page
--------------------------------------------------------------------------------
OBJECT STATE:
- Static arrays describe event details, schedule windows, review flow, and CTAs.

EXECUTION:
- Root layout renders global metadata for SurgeVector Hackathon.
- Landing route presents the hackathon itself, not a website/product explainer.
- Primary CTA sends participants to `/register/idea`.
- Secondary CTAs send volunteers to `/register/volunteer` and coordinators to `/admin`.

MEMORY:
- Static server component tree.
- No Supabase client, auth session, or form state is created.

NEXT: Participant idea submission.

STEP 2: Participant submits an idea
--------------------------------------------------------------------------------
OBJECT STATE:
- React Hook Form initializes with empty participant and idea fields.
- Zod schema enforces required details and length limits.

EXECUTION:
- User enters full name, email, organization, department, idea title, problem,
  proposed solution, and AI usage notes.
- Client validation blocks incomplete or malformed input.
- Server action validates the payload again.
- Server action inserts an `idea_submissions` row with status `submitted`.
- Server action writes an audit log entry for the submitted idea.

MEMORY:
- Client form state moves idle -> submitting -> success/error.
- Database gains one submitted idea and one audit log row on success.
- No team or mentor assignment is created during idea intake.

NEXT: Admin/panel review outside this form.

STEP 3: Approved idea proceeds to team registration
--------------------------------------------------------------------------------
OBJECT STATE:
- Existing idea submission has status `approved`.
- Team form initializes with one member by default.

EXECUTION:
- User enters approved idea ID, team name, organization, and one to four members.
- Client validation enforces minimum one member and maximum four members.
- Server action validates the payload again.
- Server action checks that the idea exists and is approved.
- Server action creates a team linked to the idea.
- Server action creates team member rows and an audit log entry.

MEMORY:
- Database gains one team and one to four team member rows.
- Mentor assignment remains a later admin/mentor action.

NEXT: Mentor coordination in Phase 1 admin/mentor management.

STEP 4: Invalid team registration attempt
--------------------------------------------------------------------------------
OBJECT STATE:
- Idea ID is missing, malformed, not found, or not approved.
- Member list may be empty or above four entries.

EXECUTION:
- Malformed IDs and invalid member counts fail Zod validation.
- Existing but unapproved ideas fail the server-side approved-status check.
- No database writes occur on validation failure.

MEMORY:
- Existing idea submission is unchanged.
- No team or team member rows are inserted.

================================================================================
ISSUES IDENTIFIED
================================================================================
P0 Critical:
- None.

P1 High:
- Branding must use SurgeVector Hackathon everywhere touched.
- Team creation must be blocked unless the idea is approved.
- Server-side validation is authoritative because client validation can be bypassed.

P2 Medium:
- Landing copy should show hackathon details and participant actions, not describe
  what the website is about.
- Review remains a simple Phase 1 status flow; do not add super-admin hierarchy,
  advanced judging, realtime systems, or AI copilots.
- Missing Supabase service configuration must return a friendly form error.

P3 Low:
- Admin review UI can be added when the admin dashboard phase is implemented.

TODOs:
- Implement idea intake form and server action.
- Implement approved-idea team registration form and server action.
- Update docs and run lint/typecheck.

================================================================================
VOLUNTEER REGISTRATION SIMULATION
================================================================================
Date: 2026-05-13
Test Case: Phase 1 volunteer registration
Command: Manual paper-based simulation before implementation and validation

================================================================================
INITIAL STATE
================================================================================
ENVIRONMENT:
- Next.js App Router route `/register/volunteer`
- React Hook Form and Zod available for form validation
- Supabase writes happen through a server action

FILE SYSTEM:
- `src/app/register/volunteer/page.tsx` currently renders a placeholder.
- `src/validations/volunteer.ts` already describes the volunteer payload.
- `supabase/migrations/001_phase_1_initial_schema.sql` defines
  `volunteer_registrations`.
- `docs/ISSUES_AND_TODOS.md` records persistent findings.

================================================================================
EXECUTION FLOW - STEP BY STEP
================================================================================

STEP 1: Render volunteer registration route
--------------------------------------------------------------------------------
OBJECT STATE:
- Form default values are empty strings for contact fields and notes.
- Preferred roles starts as an empty string array.

EXECUTION:
- Route renders explanatory Phase 1 volunteer copy.
- Client form initializes with `volunteerRegistrationSchema`.
- No Supabase client is created during static route render.

MEMORY:
- Client form state is idle.
- Database state is unchanged.

NEXT: User submits a valid volunteer registration.

STEP 2: Submit valid volunteer registration
--------------------------------------------------------------------------------
OBJECT STATE:
- Payload contains full name, email, department, one or more preferred roles,
  and optional availability notes.

EXECUTION:
- Client Zod validation passes.
- Server action validates the same payload again.
- Server action inserts one `volunteer_registrations` row with status
  `submitted`.
- Server action writes one `audit_logs` row with action `submitted`.
- Form shows success and resets to default values.

MEMORY:
- Form state moves idle -> submitting -> success.
- Database gains one volunteer row and one audit row.
- No scheduling, shift, assignment, approval, realtime, or future-phase state is
  created.

NEXT: Handle invalid or failed submissions.

STEP 3: Invalid volunteer registration
--------------------------------------------------------------------------------
OBJECT STATE:
- Required fields may be missing, email malformed, preferred roles empty, or
  notes oversized.

EXECUTION:
- Client validation reports field-level errors.
- If bypassed, server validation returns a generic correction message.
- No Supabase write occurs on invalid payloads.

MEMORY:
- Existing volunteer and audit rows remain unchanged.

STEP 4: Supabase or audit logging failure
--------------------------------------------------------------------------------
OBJECT STATE:
- Valid payload reaches the server action.
- Supabase service role configuration may be missing, or a database insert may
  fail.

EXECUTION:
- Missing service role key returns a friendly form error.
- Volunteer insert failure returns the database error.
- Audit log failure is surfaced clearly after the volunteer row is inserted.

MEMORY:
- Partial audit failure does not create assignment or scheduling side effects.

================================================================================
ISSUES IDENTIFIED
================================================================================
P0 Critical:
- None.

P1 High:
- Server-side validation is authoritative because client validation can be
  bypassed.
- Invalid submissions must not create partial volunteer or audit rows.
- Audit logging should happen immediately after a successful volunteer insert.

P2 Medium:
- Preferred roles should stay in the existing text array column; do not add
  assignment, shift, or scheduling systems.
- Missing Supabase service configuration must return a friendly form error.

P3 Low:
- Volunteer status remains `submitted`; approval and coordination screens are
  out of scope unless explicitly requested later.

TODOs:
- Implement volunteer server action and form.
- Wire `/register/volunteer` to the real Phase 1 route.
- Update architecture and volunteer docs.
- Run lint and typecheck.

================================================================================
END OF SIMULATION
================================================================================
